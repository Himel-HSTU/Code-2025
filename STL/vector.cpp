#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main ()
{
ios::sync_with_stdio(false);
cin.tie(0);
    int a[5];
    a.fill(10);
    for (auto it = a.begin(); it  !=a.end(); it++)
    {
       cout<<" "<<*it;
    }
    return 0;
}